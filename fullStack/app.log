::1 - - [21/May/2026:17:14:12 +0000] "GET / HTTP/1.1" 200 0::1 - - [21/May/2026:17:16:28 +0000] "GET / HTTP/1.1" 500 1189
("fs")

dotenv.config()

const app = express()

const port = process.env.PORT || 6700

// middleware
app.use(
    morgan('common', {
        stream: fs.createWriteStream('./app.log', { flags: 'a' })
    })
)

// static folder
app.use(express.static(__dirname + '/public'))

// html file path
app.set('views', './src/views')

// view engine
app.set('view engine', 'ejs')

// import routers
let categoryRouter = require('./src/controller/CategoryRouter')
let productRouter = require('./src/controller/ProductRoute')

// default route
app.get('/', (req, res) => {
    res.render('index',{title:'ejs code file'})
})

// use routers
app.use('/category', categoryRouter)
app.use('/product', productRouter)

// server
app.listen(port, () => {
    console.log(`Server is running on port ${port}`)
})